<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.royalty.backend.auth.mapper.UserMapper">

    <!-- =========================
         ResultMap
         ========================= -->
    <resultMap id="UserMap" type="com.royalty.backend.auth.domain.User">
        <id property="id" column="user_id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="role" column="role"/>
        <result property="provider" column="provider"/>
        <result property="providerId" column="provider_id"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <!-- =========================
         SELECT
         ========================= -->

    <select id="findByUsername" resultMap="UserMap">
        SELECT *
        FROM users
        WHERE username = #{username}
    </select>

    <select id="findById" resultMap="UserMap">
        SELECT *
        FROM users
        WHERE user_id = #{id}
    </select>

    <select id="findByProviderId" resultMap="UserMap">
        SELECT *
        FROM users
        WHERE provider = #{provider}
          AND provider_id = #{providerId}
    </select>

    <select id="existsByUsername" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE username = #{username}
    </select>

    <!-- =========================
         INSERT
         ========================= -->
    <insert id="save"
            parameterType="com.royalty.backend.auth.domain.User"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO users (
            username,
            password,
            email,
            role,
            provider,
            provider_id
        ) VALUES (
            #{username},
            #{password},
            #{email},
            #{role},
            #{provider},
            #{providerId}
        )
    </insert>

    <!-- =========================
         DELETE
         ========================= -->
    <delete id="deleteById">
        DELETE FROM users
        WHERE user_id = #{id}
    </delete>

</mapper>
