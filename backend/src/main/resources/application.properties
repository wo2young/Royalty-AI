# ==========================================
# 1. DB 연결 설정 (AWS RDS Royalty_Team)
# ==========================================
# [변경됨] AWS 접속 주소 (SSL 필수)
#spring.datasource.url=jdbc:postgresql://royalty.czikksygigvr.ap-northeast-2.rds.amazonaws.com:5432/postgres?sslmode=require
#
## [변경됨] AWS 계정 정보
#spring.datasource.username=postgres
#spring.datasource.password=e9MbrDRLAqp2AES
#spring.datasource.driver-class-name=org.postgresql.Driver

spring.datasource.url=jdbc:postgresql://localhost:5433/royalty
spring.datasource.username=postgres
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# ==========================================
# [추가] AI 모델 서버 주소 (Docker 연결용)
# ==========================================
# 백엔드가 AI 컨테이너(포트 8000)와 통신할 때 사용
ai.model.url=http://localhost:8000

# ==========================================
# 2. MyBatis 설정 (기존 유지)
# ==========================================
# XML 매퍼 파일 위치 (resources/mapper 폴더 아래 모든 xml)
mybatis.mapper-locations=classpath:mapper/**/*.xml

# (중요) DB의 user_id를 자바의 userId로 자동 변환
mybatis.configuration.map-underscore-to-camel-case=true

# 쿼리 결과가 null이어도 Setter 호출 (NullPointerException 방지)
mybatis.configuration.call-setters-on-nulls=true

# (선택) DTO 패키지 경로 (팀원들이 정한 패키지명으로 수정 필요!)
# mybatis.type-aliases-package=com.royalty.backend.dto

# ==========================================
# 3. 로그 레벨 설정 (기존 유지)
# ==========================================
# 전체적인 로그는 INFO (너무 많이 나오지 않게)
logging.level.root=INFO

# 우리 프로젝트 코드는 상세하게 (DEBUG)
logging.level.com.royalty.backend=DEBUG

# MyBatis가 실행하는 쿼리문과 파라미터를 보고 싶을 때
logging.level.com.royalty.backend.mypage.mapper=TRACE

# 스프링 빈 생성 과정에서 에러가 날 때 더 자세히 보기
logging.level.org.springframework.beans.factory=DEBUG

# SQL 출력 포맷 설정 (콘솔에 예쁘게 나오게 함)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#GPT API
openai.api.key=${OPENAI_API_KEY}
openai.model=gpt-4o
openai.api.url=https://api.openai.com/v1/chat/completions




# ==========================================
# 3. JWT 설정
# ==========================================
# ⚠️ HS256은 최소 32바이트 이상 필수
jwt.secret=ROYALTY_JWT_SECRET_KEY_32BYTE_MINIMUM!!

# Access Token: 30분
jwt.access-token-expire-ms=1800000

# Refresh Token: 14일
jwt.refresh-token-expire-ms=1209600000


# ==========================================
# 4. Kakao OAuth 설정 (🔥 추가된 부분)
# ==========================================
# 카카오 개발자 콘솔 > REST API 키
kakao.client-id=09913329b4042aed36b560de4498fac1

# 카카오 개발자 콘솔에 등록한 Redirect URI
kakao.redirect-uri=http://localhost:5173/oauth/kakao/callback


# ==========================================
# 5. 서버 설정
# ==========================================
server.port=8080
spring.application.name=royalty-backend


# ==========================================
# 6. 로그 설정 (디버그용)
# ==========================================
logging.level.org.springframework.security=DEBUG
logging.level.com.royalty.backend=DEBUG





# ==========================================
# 7. Gmail SMTP 메일 설정 (🔥 추가)
# ==========================================



spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dydrnsv3@gmail.com
spring.mail.password=emzllggtjzvyxafj

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
